export function getRandomNum(items: Array<number>) {
    return items[Math.floor(Math.random() * items.length)];
}
export function getRandomStr(items: Array<string>) {
    return items[Math.floor(Math.random() * items.length)];
}
